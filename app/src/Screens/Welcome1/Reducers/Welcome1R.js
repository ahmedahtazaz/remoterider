const INITIAL_STATE = {
  };

export default function welcome1Reducer() {
    return function reducer(state = INITIAL_STATE, action) {
      switch (action.type) {

        default:
          return state;
      }
    }
  }